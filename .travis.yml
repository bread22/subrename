language: python
python:
  - "3.5"
  - "3.6"
cache: pip
befoer_install:
  - pip install -U pytest pytest-cov
  - pip install codecov
install:
  - pip install -r requirements.txt
script:
  - pytest --cov=./
after_success:
    - codecov